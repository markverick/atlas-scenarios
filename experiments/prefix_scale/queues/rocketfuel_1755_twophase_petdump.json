{
  "_comment": "Debug queue: rocketfuel_1755 twophase p=500 PET entry dump. Uses existing stage-1 DV snap (or generates a fresh one). Stage 2 runs with a 5-second silence window (not target-count) so the simulation truly converges, then exports a full snapshot capturing every PET entry for every node. Compare snap-stage1 vs snap-p500-final to find the exact extra entries. Run with: sudo ./jobs.sh start --fresh prefix_scale/rocketfuel_1755_twophase_petdump",
  "selector": "prefix_scale/rocketfuel_1755_twophase_petdump",
  "name": "rocketfuel_1755_twophase_petdump",
  "experiment": "prefix_scale",
  "topology": "rocketfuel_1755",
  "mode": "twophase",
  "description": "Debug: rocketfuel_1755 twophase p=500 with full PET entry dump at convergence. Uses 5-second silence window instead of target-count stop.",
  "run_root_template": "experiments/prefix_scale/results/rocketfuel_1755_twophase_petdump/{timestamp}",
  "jobs": [
    {
      "name": "build twophase",
      "cmd": "./run.sh build"
    },
    {
      "name": "stage1: DV convergence snapshot (twophase, 0 prefixes)",
      "cmd": "./run.sh sim --no-build prefix_scale --topology rocketfuel_1755 --prefix-counts 0 --core-disable-prefix-egress-replication --adv-interval 1000 --dead-interval 3000 --window 300 --snap-export {run_root}/snap-stage1-twophase.json --out {run_root}/stage1-twophase"
    },
    {
      "name": "stage2: twophase p=500 with 5s silence window + full PET dump",
      "cmd": "./run.sh sim --no-build prefix_scale --topology rocketfuel_1755 --prefix-counts 500 --core-disable-prefix-egress-replication --adv-interval 1000 --dead-interval 3000 --window 600 --conv-window 5.0 --snap-import {run_root}/snap-stage1-twophase.json --snap-export {run_root}/snap-p500-twophase-final.json --out {run_root}/stage2-p500-twophase"
    }
  ]
}
